# Inferrix Patches — ThingsBoard-core modification ledger

Tracks every file in upstream ThingsBoard that Inferrix features have modified.
The purpose is **merge survival**: after each `git merge upstream/release-4.3`,
walk this ledger and verify every patch is still present (an upstream rewrite
of a file can silently revert our changes).

- **Baseline:** `upstream/release-4.3` (client version 4.3.1.3, last merged `684abbf911` on 2026-07-08; upstream tip `1053516151`). Upstream periodically **rebuilds** this branch's history, so each sync re-reconciles against a 2017 merge base (~68 conflicts) rather than a routine fast-forward — expect Bucket-A ledger conflicts, Bucket-B take-theirs, delete/delete removals, and spurious `wl/*.java` rename/rename conflicts.
- **Integration branch:** `inferrix-release-4.3`
- **Overlay paths (additive, not tracked here):** `inferrix-reporting/`, `ui-ngx/src/inferrix/`, `.claude/`, `INFERRIX.md`

A file goes in this ledger only if it existed in upstream and we changed it.
Wholly new files/directories under Inferrix-owned paths don't belong here.

---

## Feature: Reporting & Scheduling

- **Module:** `inferrix-reporting/` (new Spring Boot auto-config module; includes `scheduler/` package — `NextRunCalculator`, `ReportSchedulerService`)
- **UI:** `ui-ngx/src/inferrix/modules/reporting/` (new Angular overlay)
- **Status:** committed on `master` (commits `3cc5c003c0`, `86ba517d39`, `2df88f6c7b`). **Not yet on `inferrix-release-4.3`** — needs forward-port.

### TB-core files modified (6)

| # | File | Change | Why |
|---|------|--------|-----|
| 1 | `pom.xml` | (a) add `<module>inferrix-reporting</module>` to `<modules>` block; (b) add `<exclude>inferrix-reporting/**</exclude>` to `license-maven-plugin` excludes | Register the new Maven module + exempt Inferrix-owned code from Apache license header check |
| 2 | `application/pom.xml` | Add `<dependency>` on `com.inferrix:inferrix-reporting:${project.version}` | Pull the reporting module into the boot jar |
| 3 | `ui-ngx/tsconfig.json` | Add `"@inferrix/*": ["src/inferrix/*"]` to `compilerOptions.paths` | Path alias for Inferrix-overlay imports |
| 4 | `ui-ngx/src/app/core/services/menu.models.ts` | (a) add `reports` to `MenuId` enum; (b) add `MenuId.reports → 'Reports'` entry to `menuSectionMap`; (c) add `{id: MenuId.reports}` after `dashboards` in TENANT_ADMIN section of `defaultUserMenuMap` | Surface Reports in the left sidebar for tenant admins |
| 5 | `ui-ngx/src/app/modules/home/pages/home-pages.module.ts` | (a) `import { InferixReportingModule } from '@inferrix/modules/reporting/inferrix-reporting.module'`; (b) add `InferixReportingModule` to NgModule `exports` array | Register the reporting Angular module so its routes/components are available |
| 6 | `ui-ngx/yarn.lock` | Regenerated on `yarn install` | Auto-managed; no manual merge needed beyond `yarn install` after sync |

### Merge-recovery procedure

If any of the above show as conflicted or reverted after merging `upstream/release-4.3`:

1. **`pom.xml`** — check that `<module>inferrix-reporting</module>` and the license exclude are still present. Re-apply if missing.
2. **`application/pom.xml`** — check that the `com.inferrix:inferrix-reporting` dependency block is still present in `<dependencies>`. Re-apply if missing.
3. **`ui-ngx/tsconfig.json`** — verify the `@inferrix/*` path alias survives. If upstream restructured `paths`, re-insert.
4. **`ui-ngx/src/app/core/services/menu.models.ts`** — three insertion points; verify all three (enum entry, sectionMap entry, defaultUserMenuMap reference). Upstream regularly adds new menu items to these structures, so this is the highest-risk file in the ledger.
5. **`ui-ngx/src/app/modules/home/pages/home-pages.module.ts`** — verify the import and the entry in `exports`. Upstream may add new modules to the same arrays, so check ordering doesn't drop our entry.
6. **`ui-ngx/yarn.lock`** — run `yarn install` to regenerate; no manual merge.

---

## Feature: White Labeling

- **New overlay paths (additive, not tracked below):**
  - `common/data/.../wl/*` (domain models: `WhiteLabeling`, `WhiteLabelingParams`, `LoginWhiteLabelingParams`, `Palette`, `PaletteSettings`, `Favicon`, `WhiteLabelingType`)
  - `dao/.../wl/*` + `dao/.../sql/wl/*` (DAO + JPA repo + entity)
  - `application/.../controller/WhiteLabelingController.java`
  - `ui-ngx/.../core/http/white-labeling.service.ts`, `.../core/services/white-labeling-runtime.service.ts`
  - `ui-ngx/.../home/pages/white-labeling/*` (Angular page + sub-components)
  - `ui-ngx/.../shared/models/white-labeling.models.ts`
- **Status:** 15 commits on `inferrix-release-4.3` — fully committed. Latest: `2248c8bc55 feat(wl): add permission wiring, transactional cache, and legal-content support`.

### TB-core files modified (23)

#### Backend (10)

| # | File | Change | Why |
|---|------|--------|-----|
| 1 | `application/src/main/data/upgrade/basic/schema_update.sql` | Append `CREATE TABLE IF NOT EXISTS white_labeling (...)` block at end, wrapped in `-- WHITE LABELING TABLE START/END` comments | Upgrade path for existing installs |
| 2 | `application/src/main/java/org/thingsboard/server/controller/BaseController.java` | (a) `import org.thingsboard.server.dao.wl.WhiteLabelingService;` near other dao imports; (b) `@Autowired protected WhiteLabelingService whiteLabelingService;` field, after `apiKeyService` | Make WL service available to all controllers via inheritance |
| 3 | `application/src/main/java/org/thingsboard/server/service/security/permission/Resource.java` | Add `WHITE_LABELING,` enum entry between `MOBILE_APP_SETTINGS` and `JOB(EntityType.JOB)` | Declare the WL resource for permission checks |
| 4 | `application/src/main/java/org/thingsboard/server/service/security/permission/CustomerUserPermissions.java` | `put(Resource.WHITE_LABELING, new PermissionChecker.GenericPermissionChecker(Operation.READ, Operation.WRITE));` after `MOBILE_APP_SETTINGS` entry | Customer users can read/write their own WL settings |
| 5 | `application/src/main/java/org/thingsboard/server/service/security/permission/SysAdminPermissions.java` | `put(Resource.WHITE_LABELING, PermissionChecker.allowAllPermissionChecker);` after `MOBILE_APP_SETTINGS` entry | Sys admin full WL control |
| 6 | `application/src/main/java/org/thingsboard/server/service/security/permission/TenantAdminPermissions.java` | `put(Resource.WHITE_LABELING, PermissionChecker.allowAllPermissionChecker);` after `MOBILE_APP_SETTINGS` entry | Tenant admin full WL control |
| 7 | `application/src/main/resources/thingsboard.yml` | Add `whiteLabeling` block under `cache.specs:` with `timeToLiveInMinutes` and `maxSize` envs `CACHE_SPECS_WHITE_LABELING_TTL` / `CACHE_SPECS_WHITE_LABELING_MAX_SIZE` | WL cache config (cache name matches `CacheConstants.WHITE_LABELING_CACHE`) |
| 8 | `common/data/src/main/java/org/thingsboard/server/common/data/CacheConstants.java` | Add `public static final String WHITE_LABELING_CACHE = "whiteLabeling";` as the last constant | Cache name constant referenced by DefaultWhiteLabelingService |
| 9 | `dao/src/main/java/org/thingsboard/server/dao/model/ModelConstants.java` | Add a `White labeling constants.` block with `WHITE_LABELING_TABLE_NAME`, `WHITE_LABELING_SETTINGS_PROPERTY`, `WHITE_LABELING_TYPE_PROPERTY`, `WHITE_LABELING_DOMAIN_ID_PROPERTY`, after the API key constants block (~line 762) | Schema constants for the JPA entity |
| 10 | `dao/src/main/resources/sql/schema-entities.sql` | Append `CREATE TABLE IF NOT EXISTS white_labeling (...)` at end of file | Schema for fresh installs |

#### Frontend (13)

| # | File | Change | Why |
|---|------|--------|-----|
| 11 | `ui-ngx/src/app/core/auth/auth.service.ts` | (a) import `WhiteLabelingHttpService` and `WhiteLabelingRuntimeService`; (b) inject both in constructor; (c) in `notifyAuthenticated`, call `wlHttp.getWhiteLabelParams({ignoreErrors:true}).subscribe(params => wlRuntime.applyParams(params), () => {})` | Load tenant WL params right after login |
| 12 | `ui-ngx/src/app/core/http/public-api.ts` | Append `export * from './white-labeling.service'` | Public-API surface for the WL HTTP service |
| 13 | `ui-ngx/src/app/core/services/menu.models.ts` | **Five insertion points** (highest-risk file): (a) `white_labeling = 'white_labeling'` in `MenuId` enum; (b) `menuSectionMap` entry mapping `MenuId.white_labeling` → name `'white-labeling.white-labeling'`, path `/settings/whiteLabeling`, icon `format_paint`; (c) add `{id: MenuId.white_labeling}` to SYS_ADMIN settings section; (d) add `{id: MenuId.white_labeling}` to TENANT_ADMIN settings section; (e) add new `{id: MenuId.settings, pages: [{id: MenuId.white_labeling}]}` block to CUSTOMER_USER menu | Sidebar entry for sysadmin, tenant admin, and customer user |
| 14 | `ui-ngx/src/app/core/services/title.service.ts` | (a) import + inject `WhiteLabelingRuntimeService`; (b) in `setTitle`, compute `const appTitle = wlRuntime.currentAppTitle || env.appTitle` and use `appTitle` in both `title.setTitle` calls | Dynamic browser tab title from WL settings |
| 15 | `ui-ngx/src/app/modules/home/components/dashboard-page/dashboard-page.component.ts` | (a) import + inject `WhiteLabelingRuntimeService`; (b) in `ngOnInit`, push `wlRuntime.logo$.subscribe(url => this.defaultDashboardLogo = url)` into `rxSubscriptions` (must be FIRST entry — runs before route.data subscribe) | Dynamic dashboard logo |
| 16 | `ui-ngx/src/app/modules/home/home.component.ts` | (a) import + inject `WhiteLabelingRuntimeService`; (b) in `ngOnInit`, subscribe to `wlRuntime.logo$.pipe(takeUntil(this.destroy$))` and set `this.logo = url` (must be FIRST line in ngOnInit) | Dynamic sidebar logo |
| 17 | `ui-ngx/src/app/modules/home/pages/admin/admin-routing.module.ts` | (a) import `WhiteLabelingComponent`; (b) on `/settings` route data: add `Authority.CUSTOMER_USER` to both `auth` arrays (parent + index child) and add `CUSTOMER_USER: '/settings/whiteLabeling'` to `redirectTo`; (c) add a new child route `{path: 'whiteLabeling', component: WhiteLabelingComponent, ...}` after `...aiModelRoutes` | Customer users can hit `/settings/whiteLabeling`; nests WL page under the existing settings shell |
| 18 | `ui-ngx/src/app/modules/home/pages/home-pages.module.ts` | (a) `import { WhiteLabelingModule } from '@home/pages/white-labeling/white-labeling.module';` (b) add `WhiteLabelingModule` to NgModule `exports` | Register WL page module |
| 19 | `ui-ngx/src/app/modules/login/pages/login/login.component.html` | Replace `<tb-logo link="https://thingsboard.io" target="_blank" class="login-logo">` with `<tb-logo [src]="loginLogo" target="_blank" class="login-logo">` | Login screen logo is data-bound, not hardcoded |
| 20 | `ui-ngx/src/app/modules/login/pages/login/login.component.ts` + `.html` | (a) import `WhiteLabelingHttpService`, `WhiteLabelingRuntimeService`, `LoginWhiteLabelingParams`; (b) add `loginLogo`, `loginLogoHeight`, `loginCardColor`, `pageBackgroundColor`, `darkForeground` fields with defaults; (c) inject the two WL services; (d) in `ngOnInit`, call `wlHttp.getLoginWhiteLabelParams` and apply params + assign fields; (e) in the template: `tb-logo` gets `[src]` + `[style.height.px]`, the outer `.tb-login-content` div gets `[class.tb-dark]="!darkForeground"` (static `tb-dark` class removed) + `[style.background-color]="pageBackgroundColor"`, and the `mat-card` gets `[style.background-color]="loginCardColor"` | Login page reads its own WL params before authentication and applies logo/size/card color/page background |
| 20b | `ui-ngx/src/app/shared/components/logo.component.scss` | Inside the `:host-context(.login-logo)` block add `img { object-fit: contain; }` | Custom login logos keep aspect ratio in the fixed 280px-wide slot |
| 21 | `ui-ngx/src/app/shared/components/footer.component.html` | Replace single `<small>Copyright © {{year}} The ThingsBoard Authors</small>` with a conditional `*ngIf="(showNameVersion$ \| async) && (platformName$ \| async) as name; else defaultFooter"` and a `#defaultFooter` template that preserves the original | Footer text comes from WL settings when available |
| 22 | `ui-ngx/src/app/shared/components/footer.component.ts` | (a) import + inject `WhiteLabelingRuntimeService`; (b) expose `showNameVersion$ = wlRuntime.showNameVersion$` and `platformName$ = wlRuntime.platformName$` as fields | Bind the observables consumed by the template |
| 23 | `ui-ngx/src/assets/locale/locale.constant-en_US.json` | Add the top-level `"white-labeling": { ... }` i18n section (inserted between `version-control` and `widget`) containing the menu label key `white-labeling.white-labeling` plus all WL page labels | i18n for the White-labeling menu entry, breadcrumb, and settings page. (Was dropped once already — 2026-07 merge — and rediscovered on the deployed server as raw `white-labeling.white-labeling` text. The earlier row text wrongly said a single key in a `functions` section; the code references `white-labeling.*` keys.) This is the ONLY Inferrix content in a 10k-line file upstream churns constantly, so it is trivially dropped on merge — the merge guard must grep this file's "ours" side for `white-labeling` before take-theirs |

### Merge-recovery procedure (WL-specific notes)

- **`menu.models.ts`** is the single highest-risk file in the whole ledger (5 insertion points; upstream regularly touches these maps). Always diff this file by hand after merging.
- **`BaseController.java`** is a hub class — upstream adds `@Autowired` fields here frequently. Make sure our `whiteLabelingService` field survives.
- **`admin-routing.module.ts`** — three sub-changes on the same `/settings` route. Verify all three (`auth` array, `redirectTo` map, new child route).
- **`Resource.java`** — upstream adds enum values here regularly. Verify `WHITE_LABELING,` survives in its expected position (between `MOBILE_APP_SETTINGS` and `JOB`).
- **`schema_update.sql`** and **`schema-entities.sql`** both create the `white_labeling` table — confirm both still contain the block (upstream may rewrite ordering).
- **`locale.constant-en_US.json`** — the `functions.white-labeling` key is the only Inferrix content in this 10k-line file; upstream churns it every release and it does NOT stand out by name in a big diff. The merge procedure greps each Bucket-B "ours" side for `inferrix|whitelabel|white-labeling|...` precisely to catch this file before a blind take-theirs. Keep that guard.

---

## Feature: Default Inferrix Cortex Branding

Bakes Inferrix branding into the default build (every `yarn build:prod` output is Inferrix-branded — no white-labeling runtime config involved). Brand colors: primary blue `#2b388f`, red `#ec1c24` (sampled from `docs/images/FF_INFERRIX_slogan black.png`).

| # | File | Change | Why |
|---|------|--------|-----|
| B1 | `ui-ngx/src/assets/logo_title_white.svg` | Entire file replaced with an SVG-wrapped white-text variant of the Inferrix slogan logo (blue→white, red kept; embedded base64 PNG) | Default logo for login page, sidebar, dashboards — rendered on dark chrome |
| B2 | `ui-ngx/src/assets/logo_white.svg` | Entire file replaced with the icon-only white variant (radial mark) | Icon-only brand mark |
| B3 | `ui-ngx/src/thingsboard.ico` | Binary replaced with Inferrix favicon (`docs/images/favicon.ico`) | Browser tab icon; filename kept so `index.html` link and angular.json assets need no change |
| B4 | `ui-ngx/src/index.html` | `<title>ThingsBoard</title>` → `<title>Inferrix Cortex</title>` | Initial tab title before Angular boots |
| B5 | `ui-ngx/src/environments/environment.ts` + `environment.prod.ts` | `appTitle: 'ThingsBoard'` → `appTitle: 'Inferrix Cortex'` | Runtime page titles |
| B6 | `ui-ngx/src/scss/constants.scss` | `$tb-primary-color: #2b388f`, `$tb-secondary-color: #4553a8`, `$tb-hue3-color: #b0b7e6` | Theme primary (light 500/600, dark surface 800 all derive from these) |
| B7 | `ui-ngx/src/theme.scss` | New `$tb-mat-inferrix-red` palette map (500 `#ec1c24`) inserted before `$tb-primary`; `$tb-accent` now `mat.m2-define-palette($tb-mat-inferrix-red)` instead of deep-orange | Accent color = brand red |
| B8 | 13 component files: `script-lang`, `markdown`, `widget-button-toggle`, `alarm-rule-filter-text`, `image-cards-select`, `getting-started-widget`, `getting-started-completed-dialog`, `recent-dashboards-widget`, `rate-limits-text`, `filter-text`, `rulechain-page`, `device-check-connectivity-dialog` (.scss) + `notification.component.ts`, `sent-table-config.resolver.ts` | Hardcoded `#305680` → `#2b388f` | Stray old-primary accents in component styles |

| B9 | `ui-ngx/src/app/modules/home/home.component.html` | Removed the `<tb-github-badge class="lt-md:!hidden">` line from the primary toolbar | No "Star on GitHub" button in a branded product |
| B10 | `ui-ngx/src/app/modules/home/home.component.scss` | `.tb-logo-title` height `36px` → `52px` | Larger sidebar logo (header is 64px tall) |
| B11 | `ui-ngx/src/app/modules/home/components/dashboard-page/dashboard-page.component.html` | Dashboard footer span: `Powered by <a href="https://thingsboard.io">ThingsBoard v.{{ thingsboardVersion }}</a>` → `Powered by <a href="https://www.inferrix.com">Inferrix Cortex v.{{ thingsboardVersion }}</a>` | Branded dashboard footer linking to inferrix.com |
| B12 | `ui-ngx/src/assets/locale/locale.constant-en_US.json` | Both `dashboard.socialshare-*` values: "powered by ThingsBoard" → "powered by Inferrix Cortex" | Branded share text (same merge-guard caveat as row 23) |
| B13 | `application/src/main/java/org/thingsboard/server/service/install/DefaultSystemDataLoaderService.java` | Default account emails `sysadmin/tenant/customer/customerA/customerB/customerC@thingsboard.org` → `@inferrix.com` (6 `createUser` calls, ~lines 201/353/367-370) | Fresh installs create Inferrix-domain logins. Existing installs must rename users via REST/DB (done on .77 on 2026-07-13) |
| B14 | `TwoFactorAuthConfigController.java`, `LoginRequest.java` (application), `AuditLog.java` (common/data) | Swagger `example` email strings `tenant@thingsboard.org` / `separate-email-for-2fa@thingsboard.org` → `@inferrix.com` | API docs show Inferrix-domain examples. (`OtaPackageInfo.java` `http://thingsboard.org/fw/1` URL example intentionally left) |
| B15 | `pom.xml` (root, license-maven-plugin config ~line 878) + new root file `license-header-template-thingsboard.txt` | (a) `<owner>` property → `The Inferrix Authors`; (b) `<validHeaders><validHeader>${main.dir}/license-header-template-thingsboard.txt</validHeader></validHeaders>` added before `<properties>`; (c) `<exclude>**/license-header-template*.txt</exclude>` appended to `<excludes>` (added 2026-07-15 during scheduler Task 13 verification) | New files get Inferrix-owner Apache headers; the validHeader (hardcoded `2016-2026 The Thingsboard Authors`) keeps the ~20k legacy files passing `license:check` without a repo-wide reformat. The exclude stops the plugin scanning `license-header-template-thingsboard.txt` **itself** as a source file — the plugin auto-excludes the primary `<header>` file but NOT the `<validHeader>` template, so without the exclude the root `license:check` fails ("Missing header in: license-header-template-thingsboard.txt"). On merge: keep all three (owner + validHeaders + template exclude); upstream may reorder the plugin config. **R2a amendment (Task C2):** three more `<exclude>` entries appended (next to `**/*.otf`) — `**/resources/html/**`, `**/resources/svg/**`, `**/resources/fonts/**` — so the PE-ported Thymeleaf templates / CSS / SVG / embedded TTF fonts staged at `application/src/main/resources/{html,svg,fonts}` (asset files, no source header) pass `license:check`. `.ttf` is binary and mycila skips it anyway, but `.html`/`.css`/`.svg` map to comment styles and WOULD fail without these excludes. On merge: keep them |

**Intentionally NOT changed** (persisted widget-content defaults, not chrome): `widget-action-dialog.component.ts`, `unread-notification-widget.models.ts`, `segmented-button-widget.models.ts`, `value-stepper-widget.models.ts`, and the mobile-page preview HTML mocks.

**Merge recovery:** all rows are simple value/file replacements. On conflict, keep ours for B1–B7; for B8 re-run `grep -rln '305680' ui-ngx/src/app --include='*.scss'` and re-apply the sed. The `src/inferrix/` overlay (angular.json `inferrix*` configs) still exists but is now redundant for logo/title — defaults carry the branding.

---

## Feature: Scheduler (PE replication)

- **Spec:** `docs/specs/2026-07-13-scheduler-design.md`; plan: `docs/superpowers/plans/2026-07-13-scheduler.md`
- **New additive paths (not tracked below):** `common/data/.../scheduler/*`, `common/data/.../id/SchedulerEventId.java`, `dao/.../scheduler/*`, `dao/.../sql/scheduler/*`, `dao/src/main/resources/sql/schema-inferrix.sql` (Inferrix schema overlay — see [§Inferrix schema overlay](#inferrix-schema-overlay-db-migration-mechanism)), `dao/.../model/sql/*SchedulerEvent*`, `dao/.../service/validator/SchedulerEventDataValidator.java`, `application/.../service/scheduler/*`, `application/.../service/entitiy/scheduler/*`, `application/.../controller/SchedulerEventController.java`, `ui-ngx/src/app/modules/home/pages/scheduler/*`, `ui-ngx/src/app/shared/models/scheduler-event.models.ts`, `ui-ngx/src/app/core/http/scheduler-event.service.ts`
- **Status:** complete on `inferrix-release-4.3` (2026-07-15) — all 13 plan tasks landed (scheduler feature commits `8f717d2163`..`HEAD`). Verified: backend scheduler tests green (`common/data` `Scheduler*`, `dao` `SchedulerEventServiceTest`, `application` `SchedulerEventControllerTest`+`DefaultSchedulerServiceTest`); frontend prod build green; full recursive `license:check` green; ledger audited (S1–S23 all present, no gaps/collisions/unrecorded touchpoints). **E2E (spec §13 DoD) pending a local platform run** — checklist handed to the operator. **DB migration (2026-07-15):** `scheduler_event` DDL relocated from upstream schema files (S4/S5/S6, now retired) into the Inferrix schema overlay `dao/.../sql/schema-inferrix.sql` (see §Inferrix schema overlay); verified via `SchedulerEventServiceTest` (7/7, overlay loads on the DAO test classpath), `application` compile, and `license:check`.
- **Known limitation (v1, documented not fixed):** `TenantIdLoader` (rule-engine) throws "Unexpected entity type" if a rule node attempts tenant-resolution on a `SCHEDULER_EVENT` originator. Set an explicit originator (device/asset) on events whose downstream rule chains need tenant resolution — the per-type originator picker (Task 11) makes this the normal path for updateAttributes/RPC/OTA; only custom/raw events default to the event id as originator.

### TB-core files modified

| # | File | Change | Why |
|---|------|--------|-----|
| S1 | `common/data/.../EntityType.java` | `SCHEDULER_EVENT(103)` appended after `API_KEY(44)` (comma-swap on API_KEY) | New entity type; proto number 103 matches PE |
| S2 | `common/data/.../id/EntityIdFactory.java` | `case SCHEDULER_EVENT -> new SchedulerEventId(uuid);` in `getByTypeAndUuid` after `API_KEY` case | Id factory for the new type |
| S2a | `common/data/.../id/EntityId.java` | `@DiscriminatorMapping(value = "SCHEDULER_EVENT", schema = SchedulerEventId.class)` added to the `@Schema` polymorphic discriminator list on `EntityId`, in alphabetical position before `TB_RESOURCE` | Registers `SchedulerEventId` in the Swagger polymorphic schema for `EntityId` so generated API docs/clients resolve the new id type correctly |
| S3 | `common/data/.../msg/TbMsgType.java` | `generateReport("Generate Report"),` after `REST_API_REQUEST` | Msg type fired by generateDashboardReport events (PE constant name kept lowercase for msg-type string parity) |
| S4 | ~~`dao/src/main/resources/sql/schema-entities.sql`~~ | **RETIRED 2026-07-15** — our append was reverted; `scheduler_event` DDL relocated to the Inferrix schema overlay (see [§Inferrix schema overlay](#inferrix-schema-overlay-db-migration-mechanism), seam O1). This upstream file no longer carries any Inferrix change | Migrated to the single-source Inferrix DB channel |
| S5 | ~~`dao/src/main/resources/sql/schema-entities-idx.sql`~~ | **RETIRED 2026-07-15** — the index was reverted out and relocated to the overlay (§Inferrix schema overlay) | Migrated to overlay |
| S6 | ~~`application/src/main/data/upgrade/basic/schema_update.sql`~~ | **RETIRED 2026-07-15** — the scheduler upgrade block was removed; the overlay is now applied on every upgrade via seam O2 (§Inferrix schema overlay). The unrelated `-- WHITE LABELING TABLE` block in this file is untouched | Migrated to the overlay's upgrade channel |
| S7 | `dao/src/main/java/org/thingsboard/server/dao/model/ModelConstants.java` | Add `Scheduler event constants.` block with `SCHEDULER_EVENT_TABLE_NAME`, `SCHEDULER_EVENT_TENANT_ID_PROPERTY`, `SCHEDULER_EVENT_CUSTOMER_ID_PROPERTY`, `SCHEDULER_EVENT_NAME_PROPERTY`, `SCHEDULER_EVENT_TYPE_PROPERTY`, `SCHEDULER_EVENT_SCHEDULE_PROPERTY`, `SCHEDULER_EVENT_CONFIGURATION_PROPERTY`, `SCHEDULER_EVENT_ENABLED_PROPERTY`, `SCHEDULER_EVENT_ORIGINATOR_ID_PROPERTY`, `SCHEDULER_EVENT_ORIGINATOR_TYPE_PROPERTY`, `SCHEDULER_EVENT_ADDITIONAL_INFO_PROPERTY` after the White labeling constants block (~line 772) | Schema constants for Task 4's JPA entity mapping |
| S7b | `dao/src/test/resources/application-test.properties` | Add `cache.specs.whiteLabeling.timeToLiveInMinutes=1440` + `cache.specs.whiteLabeling.maxSize=10000` after the `apiKeys` cache block | **Incidental White-Labeling gap fix** (not scheduler-specific): WL ledger row 7 added a `whiteLabeling` cache spec to the main `thingsboard.yml` but the dao-module test properties were never given the mirror entry, so every `@DaoSqlTest` that boots the full cache config fails Spring context init. Uncaught until now because the reactor baseline builds with `-DskipTests`. Surfaced while making Task 4's DAO tests green. Belongs conceptually with WL row 7 |
| S8 | `common/proto/src/main/proto/queue.proto` | `ToCoreMsg` field 11 `schedulerServiceMsg` + new `SchedulerServiceMsgProto` message (`tenantIdMSB/LSB`, `eventIdMSB/LSB`, `added`, `updated`, `deleted`) placed after `NotificationSchedulerServiceMsg` | Queue transport for scheduler add/update/delete events between core nodes; PE used field 5, which is already occupied in CE (`EdgeNotificationMsgProto`), so field 11 (next free after CE's 1–10 block, before the 50+ device-activity block) was used instead |
| S9 | `application/.../service/entitiy/EntityStateSourcingListener.java` | Add `private final Optional<SchedulerService> schedulerService;` field; add `case SCHEDULER_EVENT` arm in the `SaveEntityEvent` switch (routes to `onSchedulerEventAdded`/`onSchedulerEventUpdated` based on `event.getCreated()`) and in the `DeleteEntityEvent` switch (routes to `onSchedulerEventDeleted` via an `instanceof SchedulerEventInfo` pattern match) | Sources CRUD events from `BaseSchedulerEventService`'s `SaveEntityEvent`/`DeleteEntityEvent` publications into the scheduler engine's queue-propagation path (`DefaultSchedulerService.onSchedulerEvent*` → `sendSchedulerEvent` → `ToCoreMsg`); `Optional` so the listener still wires up on nodes where `DefaultSchedulerService` isn't a registered bean |
| S10 | `application/.../service/queue/DefaultTbCoreConsumerService.java` | Add `private final Optional<SchedulerService> schedulerService;` field + constructor param (explicit constructor, appended last); add `else if (toCoreMsg.hasSchedulerServiceMsg())` branch in the `processMsgs` dispatch chain after the `hasNotificationSchedulerServiceMsg` branch; add `forwardToSchedulerService(TransportProtos.SchedulerServiceMsgProto, TbCallback)` next to `forwardToNotificationSchedulerService` | Consumer-side half of the queue round-trip: delivers `SchedulerServiceMsgProto` (proto field 11, S8) off the `tb-core` topic to `DefaultSchedulerService.onQueueMsg`; falls back to `callback.onSuccess()` when the `Optional` is empty so an unconfigured node doesn't stall pack processing |
| S11 | `application/src/main/resources/thingsboard.yml` | Add `scheduler:` block (`min_interval: "${SCHEDULER_MIN_INTERVAL_IN_SEC:60}"`) after the `audit-log:` section (before `state:`); add `"scheduler_event": "${AUDIT_LOG_MASK_SCHEDULER_EVENT:W}"` to the `audit-log.logging-level.mask` map after `"ai_model"` | Config knob for timer-based scheduler event minimum interval (PE parity); audit-log mask entry so scheduler_event CRUD is covered by the existing audit-log masking mechanism like every other entity type |
| S12 | `application/.../service/security/permission/Resource.java` | `SCHEDULER_EVENT(EntityType.SCHEDULER_EVENT),` appended after `WHITE_LABELING,` (~line 55) | New permission resource so the generic `checkEntityId`/`checkEntity` machinery and `@PreAuthorize`-gated controller can resolve `Resource.of(EntityType.SCHEDULER_EVENT)` |
| S13 | `application/.../service/security/permission/TenantAdminPermissions.java` | `put(Resource.SCHEDULER_EVENT, tenantEntityPermissionChecker);` after the `Resource.WHITE_LABELING` line | Tenant admins get full CRUD on scheduler events, same as every other tenant-owned resource (`tenantEntityPermissionChecker` unconditionally allows CREATE and gates READ/WRITE/DELETE on tenant match) |
| S14 | `application/.../service/security/permission/CustomerUserPermissions.java` | `put(Resource.SCHEDULER_EVENT, schedulerEventPermissionChecker);` after the `Resource.WHITE_LABELING` line, plus a new `schedulerEventPermissionChecker` constant (structurally cloned from `customerEntityPermissionChecker`, scoped to `Operation.CREATE/READ/WRITE/DELETE`) | Customer users can self-service create/read/update/delete their own scheduler events (PE behavior, ported verbatim). **Deviation from the original task brief**, which specified reusing the shared `customerEntityPermissionChecker`: that checker's `GenericPermissionChecker` allowed-operations set has no `Operation.CREATE`, so `BaseController.checkEntity`'s null-id branch (`accessControlService.checkPermission(user, resource, Operation.CREATE, null, entity)`) always 403'd for a brand-new scheduler event. Broadening the shared checker would have silently changed CREATE semantics for `ASSET`/`DEVICE`/`ENTITY_VIEW`/`EDGE` too, so a dedicated checker was added instead — same tenant/customer-match logic, minus `READ_CREDENTIALS`/`CLAIM_DEVICES`/telemetry operations the scheduler controller never exercises |
| S15 | `application/.../controller/BaseController.java` | Field `protected SchedulerEventService schedulerEventService;` after `whiteLabelingService` (~line 400); helpers `checkSchedulerEventId`/`checkSchedulerEventInfoId` next to `checkEdgeId`/`checkEdgeInfoId` (~line 774); `case SCHEDULER_EVENT -> checkSchedulerEventId(...)` added to the `checkEntityId` switch (~line 665, alongside `AI_MODEL`/`API_KEY`) | Wires the Task 4 DAO service into the generic permission-gated entity lookup used by the controller and by `checkReferencedEntities`/calculated-field flows |
| S16 | `application/.../service/edge/EdgeEventSourcingListener.java` | `SCHEDULER_EVENT` added to the `case API_USAGE_STATE, EDGE, SCHEDULER_EVENT: return false;` arm in `isValidSaveEntityEventForEdgeProcessing` (~line 230) | **Found while making `SchedulerEventControllerTest.testSaveSchedulerEvent` green, not pre-planned in the brief.** `BaseSchedulerEventService.saveSchedulerEvent` (Task 4) publishes a plain `SaveEntityEvent` like every other entity's DAO save; this listener is a shared TB-core sourcing hook with a default-allow switch, so without an explicit exclusion arm it forwarded every scheduler save to `TbClusterService.sendNotificationMsgToEdge` — observable via `@SpyBean` invocation even though the call is internally a no-op (`EdgeEventType` has no `SCHEDULER_EVENT` mapping, so `sendNotificationMsgToEdge` would immediately return). Excluding it outright matches the product intent (scheduler events are not edge-syncable) and the existing pattern for `API_USAGE_STATE`/`EDGE` |
| S17 | `ui-ngx/src/app/shared/models/entity-type.models.ts` | **Three insertion points:** (a) `SCHEDULER_EVENT = 'SCHEDULER_EVENT',` appended to the `EntityType` enum after `API_KEY`; (b) an `EntityType.SCHEDULER_EVENT` entry added to the `entityTypeTranslations` map after the `API_KEY` entry (`type`/`typePlural`/`list`/`details`/`add`/`noEntities`/`search`/`selectedEntities` keys, `entity.*` + `scheduler.*` i18n keys — locale strings land in Task 9); (c) an `EntityType.SCHEDULER_EVENT` entry added to the `entityTypeResources` map after the `API_KEY` entry (`helpLinkId: 'scheduler'`) | Registers the new entity type with the generic entity-type-driven UI machinery (labels, help links) that Tasks 9–12's page/dialog/calendar components read from |
| S18 | `ui-ngx/src/app/core/http/public-api.ts` | Append `export * from './scheduler-event.service'` (no trailing semicolon, matching the file's existing style) | Public-API surface for the new `SchedulerEventService` HTTP client |
| S19 | `ui-ngx/src/app/core/services/menu.models.ts` | **Four insertion points** in the nav-menu structures only (NOT the `defaultHomeSectionMap` block further down, which has its own separate TENANT_ADMIN/CUSTOMER_USER entries keyed by `places:` arrays): (a) `scheduler = 'scheduler',` in `MenuId` enum after `version_control`; (b) a `MenuId.scheduler` entry in `menuSectionMap` after the `version_control` entry (name `scheduler.scheduler`, path `/features/scheduler`, icon `schedule`); (c) `{id: MenuId.scheduler}` appended to the TENANT_ADMIN `features` pages array (after `version_control`) in `defaultUserMenuMap`; (d) `{id: MenuId.scheduler}` inserted into the CUSTOMER_USER array in `defaultUserMenuMap`, after the `notifications_center` block | Sidebar entry for tenant admins and customer users. File is now **triple-touched** (WL row 13, reporting row 4, this row) — highest merge risk in the ledger |
| S20 | `ui-ngx/src/app/modules/home/pages/features/features-routing.module.ts` | (a) add `Authority.CUSTOMER_USER` to both the parent `features` route's `data.auth` array and the empty-path child's `data.auth` array (mirrors WL row 17's pattern); (b) `import { schedulerRoutes } from '@home/pages/scheduler/scheduler-routing.module'`; (c) `...schedulerRoutes` spread after `...vcRoutes` in the `children` array | Authorizes CUSTOMER_USER under `/features` (previously TENANT_ADMIN-only) and registers the scheduler list route. Note: the empty-path child's `redirectTo` stays a single string (`/features/otaUpdates`, TENANT_ADMIN-only content) rather than a per-authority map — no CUSTOMER_USER-facing menu entry links bare `/features`, so this is a latent (pre-existing-shape) gap only if a customer user manually navigates to `/features`, not a regression this task introduces |
| S21 | `ui-ngx/src/app/modules/home/pages/home-pages.module.ts` | (a) `import { SchedulerModule } from '@home/pages/scheduler/scheduler.module';` after the `VcModule` import; (b) add `SchedulerModule,` to the NgModule `exports` array after `VcModule,` | Registers the scheduler page module (route table config resolver + dialog) so its routes/components are available |
| S22 | `ui-ngx/src/assets/locale/locale.constant-en_US.json` | (a) new top-level `"scheduler": { ... }` section (132 keys — 126 imported verbatim from the PE jar's `.scheduler` section, plus 6 minted `scheduler.generate-report`/`generate-dashboard-report`/`update-attributes`/`send-rpc-request`/`update-firmware`/`update-software` keys using PE's literal English text — PE itself hardcodes these six as plain strings in its compiled bundle, not i18n keys, so there is no PE key to copy), inserted alphabetically between `profiles` and `security`; (b) three `entity.*` keys (`type-scheduler-event`, `type-scheduler-events`, `list-of-scheduler-events`) inserted after `list-of-ota-packages` in the `entity` section | i18n for the Scheduler menu entry, table columns, and dialog. Same merge-guard caveat as WL row 23 (10k-line file upstream churns constantly) — both insertions were done as raw text splices (not a JSON `load`/`dump` round-trip) because the file's indentation is not a clean recursive 4-space-per-depth scheme in every section; re-serializing the whole file reformats unrelated sections and balloons the diff. Re-apply the same way (locate the alphabetic/anchor insertion point, splice text, do not reformat) if dropped on merge |
| S23 | `ui-ngx/package.json` + `ui-ngx/yarn.lock` | Added 7 `@fullcalendar/*@^6.1.x` deps (`core`, `angular`, `daygrid`, `timegrid`, `list`, `interaction`, `moment`; resolved to `6.1.21`) via `yarn add` | Powers the Task 12 combined list+calendar scheduler page (`tb-scheduler-events`) — drag-to-reschedule, view switcher, event context menu. On merge conflict in `yarn.lock`, take ours for the 7 `@fullcalendar/*` entries (or re-run `yarn install` to regenerate the lockfile from `package.json`) |

### Merge-recovery procedure

- **`EntityType.java`** — verify `SCHEDULER_EVENT(103)` is still the last enum constant (comma-swapped with `API_KEY`). Upstream regularly appends new entity types here; re-apply the comma-swap if a merge drops it.
- **`EntityIdFactory.java`** — verify the `case SCHEDULER_EVENT -> new SchedulerEventId(uuid);` arm survives in `getByTypeAndUuid`. This is an exhaustive `switch` expression — if upstream adds a new `EntityType` constant, the compiler will fail loudly until every arm (including ours) is present, so a dropped case is unmissable.
- **`EntityId.java`** — verify the `@DiscriminatorMapping` for `SCHEDULER_EVENT` survives in the class-level `@Schema` annotation on `EntityId`. This is annotation metadata only (no compile-time enforcement), so a merge could silently drop it without breaking the build — check by hand.
- **`TbMsgType.java`** — verify `generateReport("Generate Report"),` survives after `REST_API_REQUEST`. Not an exhaustive switch; a merge could silently drop it — check by hand.
- **`schema-entities.sql`** / **`schema-entities-idx.sql`** / **`basic/schema_update.sql`** — as of 2026-07-15 these upstream files no longer carry `scheduler_event` (S4/S5/S6 retired). The DDL now lives in the Inferrix schema overlay — there is nothing to recover in these files; verify the overlay seams instead (see [§Inferrix schema overlay](#inferrix-schema-overlay-db-migration-mechanism) merge-recovery). If a merge re-introduces a stray `scheduler_event` block into any of them, delete it — the overlay is the single source.
- **`ModelConstants.java`** — verify the `Scheduler event constants.` block survives after the White labeling constants block. All 10 constants must be present and the generic constants (`TENANT_ID_PROPERTY`, `CUSTOMER_ID_PROPERTY`, `ADDITIONAL_INFO_PROPERTY`) must remain as references, not hardcoded strings.
- **`queue.proto`** — verify `ToCoreMsg.schedulerServiceMsg = 11` survives and that field number 11 is still unique within `ToCoreMsg`. Proto field numbers are compile-safe for us (a collision fails codegen loudly), but a merge that renumbers or reuses field 11 in `ToCoreMsg` would silently break wire compatibility between nodes running different builds — re-check the field list by hand after every upstream merge, not just on build failure.
- **`EntityStateSourcingListener.java`** — verify the `Optional<SchedulerService> schedulerService` field and both `case SCHEDULER_EVENT` switch arms survive. Neither switch is exhaustive (both have a `default -> {}`), so a dropped case fails silently — check by hand.
- **`DefaultTbCoreConsumerService.java`** — verify the `Optional<SchedulerService> schedulerService` constructor param (last positional arg) and the `hasSchedulerServiceMsg()`/`forwardToSchedulerService` dispatch branch survive. The constructor is explicit (not `@RequiredArgsConstructor`), so a merge that reorders params here is a compile error if types collide, but a silent drop of the whole param+branch pair would just mean scheduler queue messages get unhandled (falls through the `if/else if` chain with no `callback.onSuccess()/onFailure()` — the pack processing timeout would eventually cover it, but check by hand after merges touching this file).
- **`thingsboard.yml`** — verify the `scheduler:` block (`min_interval`) survives after `audit-log:`/before `state:`, and the `"scheduler_event"` mask entry survives in `audit-log.logging-level.mask` after `"ai_model"`. Pure config; no compile-time enforcement — check by hand.
- **`Resource.java`** — verify `SCHEDULER_EVENT(EntityType.SCHEDULER_EVENT),` survives in the `Resource` enum. Not exhaustive against `EntityType`; a merge could silently drop it — check by hand (breakage shows up as a 403 or an `IllegalArgumentException` from `Resource.of()` at runtime, not a compile error).
- **`TenantAdminPermissions.java`** / **`CustomerUserPermissions.java`** — verify the `Resource.SCHEDULER_EVENT` `put(...)` lines survive, and that `CustomerUserPermissions` still has its own dedicated `schedulerEventPermissionChecker` (not silently merged back onto the shared `customerEntityPermissionChecker`, which lacks `Operation.CREATE` — see S14). Pure map registration; no compile-time enforcement — check by hand.
- **`BaseController.java`** — verify the `schedulerEventService` field, `checkSchedulerEventId`/`checkSchedulerEventInfoId` helpers, and the `case SCHEDULER_EVENT -> checkSchedulerEventId(...)` switch arm all survive. The switch is exhaustive over `EntityType`, so a dropped case fails the build loudly; the field/helpers are not compiler-enforced — check by hand.
- **`EdgeEventSourcingListener.java`** — verify `SCHEDULER_EVENT` survives in the `case API_USAGE_STATE, EDGE, SCHEDULER_EVENT: return false;` arm of `isValidSaveEntityEventForEdgeProcessing`. Not exhaustive (falls through to a default-allow `return true`), so a dropped case fails silently — a scheduler save would start round-tripping through `sendNotificationMsgToEdge` again — check by hand.
- **`entity-type.models.ts`** — verify all three insertion points survive: the `SCHEDULER_EVENT` enum member, the `entityTypeTranslations` map entry, and the `entityTypeResources` map entry (both maps keyed after the `API_KEY` entry). None of these are compiler-enforced against `EntityType`'s members (both maps are populated ad hoc, not exhaustively), so a merge that reorders or drops an entry fails silently — the symptom is a blank/untranslated label or missing help link in the scheduler UI, not a build error — check by hand.
- **`public-api.ts`** — verify `export * from './scheduler-event.service'` survives at the tail. Upstream doesn't touch this file's content (Inferrix-only exports), but a merge could still drop the whole tail block if conflict resolution goes wrong — check by hand.
- **`menu.models.ts`** (S19) — verify all four insertion points survive **in the nav-menu structures** (`MenuId` enum, `menuSectionMap`, `defaultUserMenuMap`'s TENANT_ADMIN and CUSTOMER_USER entries). Do not confuse with the separate, deeper `defaultHomeSectionMap` block (its own TENANT_ADMIN/CUSTOMER_USER entries, `places:`-keyed, home-dashboard sections) — scheduler is intentionally NOT added there. None of these maps are compiler-enforced — a dropped entry fails silently (blank sidebar entry) — check by hand.
- **`features-routing.module.ts`** (S20) — verify `Authority.CUSTOMER_USER` survives in both `auth` arrays and `...schedulerRoutes` survives in the `children` spread. Not compiler-enforced — check by hand (symptom: customer users 403 on `/features/scheduler`, or the route silently vanishes).
- **`home-pages.module.ts`** (S21) — verify the `SchedulerModule` import and its `exports` entry survive. Upstream may add new modules to the same arrays — check ordering doesn't drop ours.
- **`locale.constant-en_US.json`** (S22) — verify the top-level `scheduler` section and the three `entity.type-scheduler-event*` keys survive. Same merge-guard caveat as WL row 23 — grep the "ours" side for `scheduler` before a blind take-theirs on this file.
- **`package.json`** / **`yarn.lock`** (S23) — verify the 7 `@fullcalendar/*` entries survive in `package.json`'s `dependencies`. `yarn.lock` is machine-generated and routinely conflicts on any dependency-tree change; if dropped or garbled, re-run `yarn install` from `ui-ngx/` to regenerate it against the current `package.json` rather than hand-editing.

---

## Feature: Reporting (PE replication, R1)

- **Spec:** `docs/specs/2026-07-17-reporting-design.md`; plan: `docs/superpowers/plans/2026-07-17-reporting.md`
- **New additive paths (not tracked below):** `common/data/.../report/*`, `common/data/.../dashboardreport/*`, `common/data/.../id/Report{,Template}Id.java`, `common/data/.../job/Report*`, `common/data/.../job/task/Report*`, `common/data/.../notification/info/ReportGeneratedNotificationInfo.java`, `dao/.../report/*`, `dao/.../sql/report/*`, `dao/.../model/sql/*Report*`, `dao/.../service/validator/Report*`, `application/.../service/report/*`, `application/.../service/scheduler/report/InferrixSchedulerReportExecutor.java`, `application/.../controller/{Report,ReportTemplate,DashboardReport}Controller.java`, `ui-ngx/src/app/modules/home/pages/report/*`, `ui-ngx/.../core/http/report.service.ts`, `ui-ngx/.../shared/models/report.models.ts`, `ui-ngx/.../core/services/report.service.ts` (Task 12 — the browser-side report-view protocol driver, NOT the http client), `ui-ngx/src/tsconfig.spec.json` (Task 12 — new karma test tsconfig). DDL in the schema overlay (§Inferrix schema overlay).
- **Status:** complete on `inferrix-release-4.3` (2026-07-20) — all R1 plan tasks (1–27) landed. Verified: full reactor `clean install` green; **94 reporting tests** across `common/data` (10) + `dao` (13) + `application` (71), 0 failures/errors; frontend prod build green; `license:check` green; ledger audited (R1–R39 all present with merge-recovery notes, no unledgered TB-core touch); final whole-branch review clean (its one Important — ungated `/api/v2/report/request` enqueuing orphan jobs when the renderer is off — fixed in `ba51a1fecc`). **E2E (spec §17 DoD) pending a local platform run with `REPORTS_RENDERER_ENABLED=true`** — operator checklist at `.superpowers/sdd/task-27-e2e-checklist.md`. Renderer is **opt-in (default off)**. R2 items (CSV, component renderers, full template designer, rule nodes, server-side query context) explicitly out of R1 scope (spec §18).
- **Merge-guard grep:** after any upstream merge touching report-adjacent core files, grep the "ours" side for `REPORT_TEMPLATE|EntityType\.REPORT|JobType\.REPORT|ReportTemplate|reports\.renderer` before a blind take-theirs — most reporting TB-core touches are NOT compiler-enforced (see the merge-recovery bullets below).
- **R2a A5 (2026-07-22) — typed config, entity stays jsonb (amends the additive files above; no new TB-core row — all files are Inferrix-additive):** the typed `common/data/.../report/configuration/**` model (A1–A4, additive) replaced R1's `JsonNode` config passthrough. `ReportTemplate.configuration`, `ReportRequest.reportTemplateConfig`, and `job/task/ReportTask.reportTemplateConfig` are now the typed `ReportTemplateConfig` (PE shape); `ReportTemplate` copy-ctor `deepCopy` clones via `valueToTree`→`treeToValue` (collapses PE's duplicate `format` key — `@JsonTypeInfo(As.PROPERTY, property="format")` + real `getFormat()` — that a plain `writeValueAsString` would emit twice). `dao/.../model/sql/ReportTemplateEntity` keeps the jsonb `JsonNode` column (`@Convert(JsonConverter)`+`@JdbcType`) and tree-maps in both directions (`JacksonUtil.valueToTree` on write, `JacksonUtil.treeToValue(..., ReportTemplateConfig.class)` on read, null-safe). `application/.../service/report/PdfReportService.findDashboardComponent` now iterates the typed `getComponents()` and reads `((DashboardComponent)c).getConfig().getDashboardId()/getState()/getTimewindow()` (R1's flat `pageWidth` dropped — not in the typed model). `ReportController`/`ReportJobProcessor` type-flip through unchanged. FE `report-template-dialog` + `report.models.ts` now emit/read PE-nested DASHBOARD (`{type:"DASHBOARD",config:{...}}`, root `format:"PDF"`, no `pageWidth`) per Decision D1. Pre-existing R1-shape rows are converted by the `V4_3_1_4Migration` (R2a Task B1).
- **R2a C3/C5/D1/D2/D3 (2026-07-22) — server-side HTML→PDF render engine + non-data component renderers (all files Inferrix-additive under `application/.../service/report/**`; no new TB-core row):** the R1 render interface `TbReportRenderService.generateReport(ReportTask)` gained a second arg → `generateReport(ReportTask, TbReportCtx)` (spec §5.5, the one R1 interface change). New per-render context package `application/.../service/report/context/**` — `TbReportCtx` (R2a-minimal: `tenantId, configuration, timeZone, userId, accessToken, PdfReportImageResolver, SecurityUser`; **no** data-query methods — the `ReportDataService`-backed ctx is R2b, seam commented), `TbReportCtxProvider` + `LocalTbReportCtxProvider` (`@ConditionalOnProperty(reports.renderer.enabled)`; reconstructs the `SecurityUser` via the existing public `JwtTokenFactory.parseAccessJwtToken` — **2nd consumer, `JwtTokenFactory` NOT edited** — and builds an `ImageService`-backed `ImageServiceReportImageResolver`), plus minimal `ComponentData` + `HeaderFooterRenderLayout`. **`TbReportService`** rewritten: was `new PdfReportService(...)` in its ctor → now injects `List<TbReportRenderService>` (registers by format) + `TbReportCtxProvider`, builds the ctx and passes it to `renderService.generateReport(task, ctx)`. **`PdfReportService`** rewritten from R1's dashboard-only delegate into the component engine (`@Service @ConditionalOnProperty(reports.renderer.enabled)`, no `AbstractReportService` base — one implementor in R2a): iterates `configuration.getComponents()`, dispatches each to a `PdfReportComponentRenderer` from an `EnumMap` registry built from the injected renderer list, collects HTML fragments, measures running header/footer (`HtmlRenderUtils.measureHtmlHeight` + `measure-template.html`), assembles into `report-template.html` (page size/orientation/margins/background from `PdfReportTemplateConfig`) and rasterises via the C4 flying-saucer/openpdf renderer (`HtmlRenderUtils.createRenderer(ctx.imageResolver, usablePageWidthPx)`) → `%PDF` → `ReportData` (name via `ReportUtils.prepareReportName`). New renderer package `application/.../service/report/renderer/**` (all stateless `@Component`): `PdfReportComponentRenderer` iface, abstract `ReportComponentWithLayoutRenderer` (wraps a fragment in `component-layout.html`), `HeadingRenderer`, `RichTextRenderer`, `DividerRenderer`, `PageBreakRenderer`, `ErrorRenderer`, abstract `AbstractImageRenderer`, `ImageRenderer`, `DashboardRenderer`; plus `application/.../service/report/util/ColorUtils.java` (CSS colour normaliser for flying-saucer). **DASHBOARD via PNG:** `PdfReportService.buildDashboardComponentData` forces `DashboardReportConfig.type="png"` and calls the **unchanged** `DashboardReportService.generateReport(tenantId, config)` (R1's Playwright path already honours `type` — the renderer's `capture()` screenshots PNG when `type=png`; **no PNG overload added to `DefaultDashboardReportService`**), then `DashboardRenderer` base64-embeds the PNG as a `data:image/png` `<img>` — so R1 dashboard reports keep working through the new engine. **R2b guards:** table (`ENTITY_TABLE`/`ALARM_TABLE`/`TIME_SERIES_TABLE`) + `SUB_REPORT` components hard-fail pre-dispatch with `ReportRenderException("table components are R2b")`; `IMAGE` `sourceType=entityKey` guards in `ImageRenderer` ("entity-key image source is R2b"); non-`ReportRenderException` per-component failures degrade to an `ErrorRenderer` box (swallowed, logged). Tests: `TbReportServiceTest` rewritten to a dispatch/persist contract (mocks render service + ctx provider), `ReportRendererWiringTest` extended to assert `TbReportService`/`PdfReportService`/`LocalTbReportCtxProvider` wire (renderer-on, no bean cycle — R1 `@Lazy` invariant on `ReportTaskProcessor→TbReportService` holds), new `PdfReportServiceTest` (real render, no browser — asserts `%PDF`, multi-page on `PAGE_BREAK`, dashboard PNG captured+embedded, running header on every page, table→R2b guard). `ReportJobProcessorTest`/`ReportControllerTest`/`DashboardReportControllerTest`/`DefaultDashboardReportServiceTest` unchanged and green. Renderer stays opt-in — platform boots renderer-off.

### TB-core files modified

| # | File | Change | Why |
|---|------|--------|-----|
| R1 | `common/data/.../EntityType.java` | `REPORT_TEMPLATE(108)`, `REPORT(109)` appended after `SCHEDULER_EVENT(103)` | New entity types; PE-exact proto numbers |
| R2 | `common/data/.../id/EntityIdFactory.java` | two `case` arms in `getByTypeAndUuid` | Id factory |
| R3 | `common/data/.../id/EntityId.java` | two `@DiscriminatorMapping` entries | Swagger polymorphic schema |
| R4 | `common/data/.../job/JobType.java` | `REPORT("Report generation")` appended after `DUMMY("Dummy job")` (comma-swap) | New job type; `DefaultJobManager` auto-discovers it (collects `JobProcessor` beans by `getType()`, builds `taskProducers` by iterating `JobType.values()` — verified, no other core-file touch needed for dispatch) |
| R5 | `common/data/.../job/JobConfiguration.java` | `@Type(name = "REPORT", value = ReportJobConfiguration.class)` added to `@JsonSubTypes` | Jackson polymorphic subtype resolution for job configuration by the `type` discriminator |
| R6 | `common/data/.../job/JobResult.java` | `@Type(name = "REPORT", value = ReportJobResult.class)` added to `@JsonSubTypes` | Jackson polymorphic subtype resolution for job result by the `jobType` discriminator |
| R7 | `common/data/.../job/task/Task.java` | `@Type(name = "REPORT", value = ReportTask.class)` added to `@JsonSubTypes` | Jackson polymorphic subtype resolution for task by the `jobType` discriminator |
| R8 | `common/data/.../job/task/TaskResult.java` | `@Type(name = "REPORT", value = ReportTaskResult.class)` added to `@JsonSubTypes` | Jackson polymorphic subtype resolution for task result by the `jobType` discriminator |
| R9 | `common/data/.../job/Job.java` | `case REPORT -> new ReportJobResult();` added to the `presetResult()` switch expression, alongside `case DUMMY -> new DummyJobResult();` | **Not in the original task brief** (which scoped 5 files) — `presetResult()`'s `switch (type) {...}` is an exhaustive Java switch *expression* over `JobType` with no `default` arm; adding `JobType.REPORT` (R4) without this case is a hard compile error in `common/data` itself (verified empirically: RED before, GREEN after). Every `Job` built with `type=REPORT` needs a fresh `ReportJobResult` to accumulate task results into, mirroring `DUMMY`'s `DummyJobResult()` |
| R10 | `dao/.../model/ModelConstants.java` | Add a `Report template constants.` block (`REPORT_TEMPLATE_TABLE_NAME`, `REPORT_TEMPLATE_NAME_PROPERTY`, `_FORMAT_PROPERTY`, `_TYPE_PROPERTY`, `_DESCRIPTION_PROPERTY`, `_CONFIGURATION_PROPERTY`) after the Scheduler event constants block (~line 787) | Schema constants for Task 7's `AbstractReportTemplateEntity`/`ReportTemplateEntity`/`ReportTemplateInfoEntity` JPA mapping; the four generic tenant/customer/external-id/additional-info columns are referenced directly via the existing generic constants (`TENANT_ID_PROPERTY`/`CUSTOMER_ID_PROPERTY`/`EXTERNAL_ID_PROPERTY`/`ADDITIONAL_INFO_PROPERTY`), not re-aliased |
| R11 | `dao/.../model/ModelConstants.java` | Add a `Report constants.` block (`REPORT_TABLE_NAME`, `REPORT_TEMPLATE_ID_PROPERTY`, `REPORT_FORMAT_PROPERTY`, `REPORT_NAME_PROPERTY`, `REPORT_USER_ID_PROPERTY`, `REPORT_DATA_PROPERTY`) directly after the `Report template constants.` block (~line 798) | Schema constants for Task 8's `AbstractReportEntity`/`ReportEntity`/`ReportInfoEntity` JPA mapping (the immutable, bytea-bearing `report` table). The generic tenant/customer columns reuse `TENANT_ID_PROPERTY`/`CUSTOMER_ID_PROPERTY` directly (not re-aliased, same idiom as R10); `REPORT_DATA_PROPERTY` documents the `data` bytea column name but is not referenced by any `@Column` mapping — `ReportRepository`'s native queries carry `"data"` verbatim per PE, and the entity intentionally never maps `data` as a JPA field (bytes only move through the dedicated native `saveData`/`getDataById` queries) |
| R12 | `application/.../service/security/permission/Resource.java` | `REPORT_TEMPLATE(EntityType.REPORT_TEMPLATE)`, `REPORT(EntityType.REPORT)` appended after `API_KEY(EntityType.API_KEY)` (~line 60) | New permission resources so the generic `checkEntityId`/`checkEntity` machinery and the Task 19–21 `@PreAuthorize`-gated controllers can resolve `Resource.of(EntityType.REPORT_TEMPLATE)` / `Resource.of(EntityType.REPORT)` |
| R13 | `application/.../service/security/permission/TenantAdminPermissions.java` | `put(Resource.REPORT_TEMPLATE, tenantEntityPermissionChecker);` + `put(Resource.REPORT, tenantEntityPermissionChecker);` after the `Resource.API_KEY` line | Tenant admins get full CRUD on their tenant's reports/templates, same as every other tenant-owned resource (`tenantEntityPermissionChecker` unconditionally allows CREATE and gates READ/WRITE/DELETE on tenant match) |
| R14 | `application/.../service/security/permission/CustomerUserPermissions.java` | `put(Resource.REPORT_TEMPLATE, reportTemplatePermissionChecker);` + `put(Resource.REPORT, reportPermissionChecker);` after the `Resource.API_KEY` line, plus two new dedicated checker constants: `reportPermissionChecker` (structurally cloned from `schedulerEventPermissionChecker`'s customer-match `hasPermission` logic, but scoped to `Operation.CREATE/READ` only — narrower than Scheduler's 4-op set, per design spec §11: reports are immutable and `DELETE` is TA-only, so `WRITE`/`DELETE` are not granted — customer-match on `HasCustomerId`) and `reportTemplatePermissionChecker` (same customer-match shape, scoped to `Operation.READ` only) | Customer users self-service generate/read/download their own reports (PE parity — on-demand download is a `Operation.CREATE` on the null-id branch of `BaseController.checkEntity`, same trap as Scheduler S14, so the shared `customerEntityPermissionChecker` — no `Operation.CREATE` in its allowed-operations set — cannot be reused). Report templates are tenant-authored (create/edit/delete stays TA-only, no CU-exposed endpoint needs it), so customer users get a separate, narrower `Operation.READ`-only checker rather than reusing `reportPermissionChecker` or the shared `customerEntityPermissionChecker` (which also grants `WRITE`) — least-privilege, and keeps the two resources' checkers independently adjustable |
| R15 | `application/.../service/security/system/SystemSecurityService.java` + `.../system/DefaultSystemSecurityService.java` | Two new interface methods `String createUserAccessToken(TenantId, UserId)` / `String createUserAccessTokenFromPublicId(TenantId, String)`; impl adds `CustomerService` + `JwtTokenFactory` constructor fields and ports PE's two like-named methods (dropping PE-only `MergedUserPermissions`/`UserPermissionsService` — CE's `SecurityUser` has no such constructor). `DefaultSystemSecurityServiceTest.java`'s constructor call updated with the 2 new mock args to keep compiling (mechanical, no behavior change) | Server-minted, password-less JWT so the report renderer (Tasks 11/13) can log the headless browser in AS the report's configured user, never rendering data that user couldn't see. Mints via the same `SecurityUser(User, boolean, UserPrincipal)` construction CE's real login/refresh paths use — `createUserAccessToken` mirrors `AbstractAuthenticationProvider#authenticateByUserId`'s shape (same `UserAuthDetails` null/enabled checks) but reads via the already-injected `userService.findUserAuthDetailsByUserId` directly rather than through `UserAuthDetailsCache`, since `DefaultSystemSecurityService` isn't an `AuthenticationProvider` and a fresh (non-cached) read is safer for a security-sensitive, low-frequency path; `createUserAccessTokenFromPublicId` mirrors `AbstractAuthenticationProvider#authenticateByPublicId` verbatim except it passes the caller's real `tenantId` into `findCustomerById` instead of the HTTP public-login filter's `SYS_TENANT_ID` placeholder, since the caller (unlike an anonymous HTTP request) already has real tenant context. R1-only: mints tokens, no server-side parse-back consumer added |
| R16 | `application/pom.xml` | Add `com.microsoft.playwright:playwright:1.49.0` to `<dependencies>`, appended after the `langchain4j-ollama` entry (~line 420, directly before the closing `</dependencies>`) | Task 11's in-process Playwright-Java renderer core (`application/.../service/report/render/PlaywrightWebReportRenderer.java` + `ReportRenderException.java`, additive path, not tracked below) replaces PE's external `tb-web-report` microservice with one shared headless-Chromium `Browser` per JVM and a per-render `BrowserContext` for crash isolation. Chromium itself is a runtime-fetched binary, not a Maven artifact — installed once via `mvn -pl application exec:java -Dexec.classpathScope=test -Dexec.mainClass=com.microsoft.playwright.CLI -Dexec.args="install chromium"`, landing in `~/Library/Caches/ms-playwright` on macOS (never committed, machine-local). Merge recovery: this is a single, self-contained `<dependency>` block with no ordering dependency on its neighbors — if upstream's `<dependencies>` list is reshuffled, re-append the Playwright block anywhere inside `<dependencies>`; no other file references its position. The renderer bean itself ships disabled (`@ConditionalOnProperty(reports.renderer.enabled=true)`, unset by default) until Task 26 wires real config, so this dependency is inert for anyone who doesn't opt in. **R2a amendment (Task C1):** four more `<dependency>` blocks appended after the Playwright entry for the server-side HTML→PDF engine — `org.thymeleaf:thymeleaf:3.1.3.RELEASE`, `org.xhtmlrenderer:flying-saucer-pdf:9.13.0` (transitively brings `flying-saucer-core:9.13.0`, `com.github.librepdf:openpdf:2.0.5`, and `batik-codec:1.19`), `com.github.weisj:jsvg:1.6.1` (SVG raster), and `org.apache.xmlgraphics:xmlgraphics-commons:2.9` (pinned to PE's version; also reachable transitively via batik-codec). **AGPL guard (spec §11, HARD REQUIREMENT):** the PDF backend is **openpdf** (LGPL/MPL, package `com.lowagie.text`) — `mvn -pl application dependency:tree` MUST show `com.github.librepdf:openpdf` and MUST contain **no** `com.itextpdf` (iText 5+ = AGPL). NOTE the artifact is `flying-saucer-pdf` (NOT `flying-saucer-pdf-openpdf` — that suffixed name was the pre-9.4.0 module name and has no 9.13.0 release; at 9.13.0 the openpdf backend lives in the plain `flying-saucer-pdf` artifact per its POM). `commons-csv` (CSV format, R2b) is NOT re-added — it is already a managed `<dependency>` in this pom (1.10.0). Merge recovery: same as the Playwright block — self-contained, order-independent; re-append anywhere in `<dependencies>` if reshuffled |
| R17 | `ui-ngx/src/app/app.component.ts` | Constructor gains `private reportService: ReportService`; `setupAuth()` gains one line, `this.reportService.loadReportParams();`, immediately before the existing `this.authService.reloadUser();` | Task 12 bootstrap: mirrors PE's `AppComponent.setupAuth()` (`reportService.loadReportParams() \|\| authService.reloadUser()`), adapted because CE's `reloadUser()` already performs the `?accessToken=` login unconditionally (PE's short-circuit avoided a double login; CE doesn't need it — `loadReportParams()` only *waits* on the same user-ready signal `setupAuth()` already produces, it never re-authenticates). Detects `?reportView=true`, then (once the user-ready store selector settles) installs the `window` `message` listener and signals `postWebReportResult` availability |
| R18 | `ui-ngx/src/app/modules/home/components/dashboard-page/dashboard-page.component.ts` | Constructor gains `private reportService: ReportService`. Three call-site changes: (a) `init()` — `this.dashboardCtx.dashboardTimewindow` assignment now prefers `reportService.reportTimewindow` when `reportService.active`, falling back to `this.dashboardConfiguration.timewindow`; (b) two new `this.reportService.onDashboardLoaded(this.reportWidgetsCount())` calls, one immediately after each existing `this.mobileService.onDashboardLoaded(...)` call (inside the `reload` queryParam `setTimeout` in `ngOnInit`, and inside `openDashboardState`'s `setTimeout`); (c) new private helper `reportWidgetsCount()` (sums `layouts.main`/`layouts.right` `LayoutWidgetsArray.size()`) | Task 12: feeds the `openReport` command's `reportTimewindow` override into the dashboard, and fires the `onDashboardLoaded(widgetsCount)` signal `waitForReportPage`'s DOM predicate depends on (PE parity — mirrors the `mobileService.onDashboardLoaded` call sites exactly, per the design spec) |
| R19 | `ui-ngx/src/app/core/ws/telemetry-websocket.service.ts` | Constructor gains `private reportService: ReportService` (5th param). `subscribe()` and `update()` each gain one call — `this.trackReportWsCommand(subscriptionCommand.cmdId)` — right after the command is pushed onto `cmdWrapper.cmds`; new private `trackReportWsCommand(cmdId)` wraps `reportService.onSendWsCommand(cmdId)` in a `reportView` guard + try/catch; `processOnMessage()` gains a `reportService.onWsCmdUpdateMessage(message)` call (guarded + try/catch) as its first statement | Task 12's websocket-data readiness tracker (`waitForWebsocketData`). CE has no PE-style `.type` string to filter "unsubscribe" commands by (typed `*Cmd` classes instead) — `subscribe()`/`update()` are structurally already subscribe-only call sites (unsubscribes go through the separate `unsubscribe()` method), so no filter is needed; `onSendWsCommand`'s own `!receiveWsData.has(cmdId)` guard (report.service.ts) makes calling it from both `subscribe()` and `update()` idempotent-safe |
| R20 | `ui-ngx/src/app/modules/home/components/widget/lib/maps/map.ts` | `TbMap` gains two protected fields — `reportService: ReportService = this.ctx.$injector.get(ReportService)` and `mapUuid: string` — plus, at the top of the constructor (before `$(containerElement).empty()`), a `reportService.reportView` guard that calls `onWaitForMap()` and adds class `tb-web-report` to `containerElement` | Task 12 map-tile readiness registration (PE parity — PE registers in the equivalent map constructor). Resolved via `WidgetContext.$injector` (existing DI escape hatch already used by `map-layer.ts` for `CustomTranslatePipe`/`ResourcesService`) rather than adding a new field to `WidgetContext` itself — smaller surface, no `widget-component.models.ts` touch |
| R21 | `ui-ngx/src/app/modules/home/components/widget/lib/maps/geo-map.ts` | In `doSetupControls()`, right after `defaultLayer.layer.addTo(this.map)`, a `reportService.reportView` guard registers `defaultLayer.layer.once('load', () => this.reportService.onMapLoaded(this.mapUuid))` | Task 12: the base tile layer's Leaflet `'load'` event (all visible tiles fetched) is the map-tile-specific readiness signal `waitForWidgetsLoaded`/`waitReportWidgets` needs — distinct from `WidgetComponent.loadingData`, which only reflects the data subscription, not tile paint |
| R22 | `ui-ngx/src/app/modules/home/components/widget/lib/maps/image-map.ts` | In `updateMaxBounds()`, in the branch that creates a new overlay (`else { this.imageOverlay = L.imageOverlay(...).addTo(this.map); ... }`), a `reportService.reportView` guard registers `this.imageOverlay.once('load', () => this.reportService.onMapLoaded(this.mapUuid))` | Task 12: image-map equivalent of R21 — no tile layer, so the `ImageOverlay`'s own `'load'` event is the readiness signal |
| R23 | `ui-ngx/angular.json` + `ui-ngx/package.json` + `ui-ngx/yarn.lock` | New additive `"test"` architect target in `angular.json` (`@angular-devkit/build-angular:karma` builder, `polyfills: [zone.js, zone.js/testing]`, `tsConfig: src/tsconfig.spec.json`, `browsers: "ChromeHeadless"`); `package.json` gains a `"test": "ng test"` script plus 7 new `devDependencies` (`karma`, `karma-chrome-launcher`, `karma-jasmine`, `karma-jasmine-html-reporter`, `karma-coverage`, `jasmine-core`, `@types/jasmine`) added via `yarn add -D`, regenerating `yarn.lock` | Task 12: this repo had **no karma/test infrastructure at all** before this task (no `test` architect target, no karma deps installed, and the one pre-existing `ui-ngx/src/app/core/auth/auth.service.spec.ts` was already broken — deprecated `TestBed.get`, zero DI providers — so it was excluded, not fixed, via `src/tsconfig.spec.json`'s own `exclude`; see that file). Wiring the harness was a precondition for the `report.service.spec.ts` karma spec the design spec's Testing Strategy §17 point 9 and this task's brief require. On `yarn.lock` merge conflict: take ours for the 7 new dev entries, or re-run `yarn install` from `ui-ngx/` (same recipe as Scheduler ledger row S23) |
| R24 | `common/data/.../notification/NotificationRequestConfig.java` | Add `private List<ReportId> reports;` directly after `sendingDelayInSec` | Task 15: `ReportJobProcessor.onJobFinished` sets this to `List.of(report.getId())` on the `NotificationRequest.additionalConfig` it dispatches once a scheduled report job completes, so the notification-delivery channel (mail, task 17) can locate/attach the finished report. Plan-ordering note: originally scoped to task 17, moved up to task 15 since `onJobFinished` is this field's first consumer |
| R25 | `rule-engine/rule-engine-api/.../TbEmail.java` | Add `private final List<ReportId> reports;` as the last field (after `html`) | Task 17: carries the notification-dispatched report id(s) from `EmailNotificationChannel` (R27) into `DefaultMailService.send` (R26) so the finished report can be attached to the outgoing email. `attachments (List<BlobEntityId>)` from the original task brief was dropped — no R1 consumer, would be speculative |
| R26 | `application/.../service/mail/DefaultMailService.java` | (a) new `private final ReportService reportService;` collaborator (dao `org.thingsboard.server.dao.report.ReportService`), appended after `rateLimitService`; (b) the private 5-arg `sendMail`'s `multipart` is now also `true` when `tbEmail.getReports()` is non-empty; (c) the pre-existing images loop (`if (multipart) { for (String imgId : tbEmail.getImages().keySet()) ... }`) is now additionally guarded by its own `tbEmail.getImages() != null` check — **a necessary correctness fix uncovered while implementing, not just an addition**: after (b), `multipart` can be `true` from reports alone while `images` stays `null`, and the un-guarded loop would NPE on `.keySet()` the first time a reports-only email is sent; (d) new reports loop in the same `if (multipart)` block, after the images loop: for each `ReportId`, `reportService.findReportById` (`log.warn` + `continue` if not found), else `reportService.getReportData` → `helper.addAttachment(report.getName(), new ByteArrayDataSource(bytes, report.getFormat().getContentType()))` + `log.info` | Task 17: resolves and attaches each report's bytes as a MIME attachment (content-type from `TbReportFormat`, e.g. `application/pdf`). `ByteArrayDataSource` requires `helper` to already be multipart, which (b) guarantees whenever reports are present |
| R27 | `application/.../service/notification/channels/EmailNotificationChannel.java` | `sendNotification` reads `NotificationRequestConfig config = ctx.getRequest().getAdditionalConfig();` (nullable) and adds `.reports(config != null ? config.getReports() : null)` to the `TbEmail.builder()` chain | Task 17: the only current production populator of `TbEmail.reports` — copies `NotificationRequest.additionalConfig.reports` (R24, set by `ReportJobProcessor.onJobFinished`) through to the outgoing email. Null-guarded because `additionalConfig` is itself nullable on `NotificationRequest` — most notifications carry no report |
| R28 | `application/.../controller/BaseController.java` | two `@Autowired` report service fields (`reportService`, `reportTemplateService`); `checkReportId`/`checkReportTemplateId` helpers; `case REPORT`/`case REPORT_TEMPLATE` arms in `checkEntityId` | Task 18: mirrors Scheduler S15 wiring for the report REST controllers (Tasks 19–21). MERGE NOTE: `checkEntityId` has a `default` arm, so a lost REPORT/REPORT_TEMPLATE arm silently routes to `entitiesService.findEntityByTenantIdAndId` with no build break — re-add both arms by inspection (full note in the merge-recovery section below) |
| R29 | `common/data/.../job/Job.java` | add `REPORT_TEMPLATE` to `SUPPORTED_ENTITY_TYPES` so `JobType.REPORT` jobs [subject = report template] pass `DefaultJobService.saveJob` entity-type validation | without it every report job — scheduled or on-demand — throws `IllegalArgumentException: Unsupported entity type` at submit; re-add after any upstream merge that rewrites this set |
| R30 | `ui-ngx/src/app/shared/models/entity-type.models.ts` | add `REPORT`/`REPORT_TEMPLATE` to `EntityType` enum + `entityTypeTranslations` + `entityTypeResources` (mirrors Scheduler S17) | report UI entity registration; merge-recovery: re-add the three entries after any upstream rewrite of these maps |
| R31 | `ui-ngx/src/app/core/http/public-api.ts` | `export * from './report.service'` | exposes the report HTTP client; merge-recovery: re-add the export line |
| R32 | `ui-ngx/src/app/modules/home/pages/home-pages.module.ts` | import + register `ReportModule` (report history page), mirroring Scheduler S21 | report history route module registration; re-add the import + array entry after any upstream rewrite of this module |
| R33 | `ui-ngx/src/app/core/services/menu.models.ts` | add `MenuId.reports`/`report_history`/`report_templates`; three `menuSectionMap` entries (`reports` toggle group, `icon: 'assessment'`, `path: '/features/reports'`; `report_history` link → `/features/reports/history`; `report_templates` link → `/features/reports/templates`); TENANT_ADMIN placement — nest `{id: MenuId.reports, pages: [{id: MenuId.report_history}, {id: MenuId.report_templates}]}` inside the existing `MenuId.features` pages group, alongside `{id: MenuId.scheduler}` | Task 25b: left-nav entry for the report history + report templates pages, mirroring Scheduler S19's placement (TENANT_ADMIN only — route `data.auth` on both pages is `[Authority.TENANT_ADMIN]`, no CUSTOMER_USER, so unlike scheduler this group is NOT added to the CUSTOMER_USER tree). **MERGE NOTE: this file is now triple-touched — White-Labeling row 13, Scheduler S19, and this row — re-apply all three additive blocks after any upstream rewrite of `menu.models.ts`** |
| R34 | `ui-ngx/src/assets/locale/locale.constant-en_US.json` | splice a new top-level `"report"` object (77 keys) between the existing `"scheduler"` and `"security"` sections — the 53 PE-sourced keys (`reporting`, `templates`, `reports`, `delete-report-title`, `download`, …) plus 24 R1-only keys the actual report/scheduler UI references that PE didn't have (`name`, `template`, `report-templates`, `report-template`, `add-report-template`, `edit-report-template`, `name-required`, `dashboard-component`, `dashboard-required`, `dashboard-state`, `description`, `template-type`, `page-width`, `page-width-required`, `page-width-min`, `delete-report-template-title`/`-text`, `delete-report-templates-title`/`-text`, `recipient-user`, `notification-targets`, `notification-target`, `notification-template`, `report-history`) | Task 25b: i18n for the report history table, report template dialog/table, and the scheduler `generateReport` form's report-specific fields (Task 25a). **MERGE NOTE: splice as raw text (do not run the file through a JSON formatter — it is not alphabetically sorted and reformatting would produce a huge spurious diff); take-ours on conflict** |
| R35 | `ui-ngx/src/app/modules/home/pages/features/features-routing.module.ts` | import `reportRoutes` from `@home/pages/report/report-routing.module` and spread `...reportRoutes` into the `features` parent route's `children`, alongside `...schedulerRoutes` | Task 25b: mounts the (now `reports`-parent-nested) report routes under `/features/reports/history` and `/features/reports/templates`; re-add the import + spread line after any upstream rewrite of this file |
| R36 | `ui-ngx/src/app/modules/home/components/dashboard-page/dashboard-page.component.ts` + `.html` | dashboard toolbar "Download report" action (button + `downloadReport` handler injecting the `@core/http` `ReportService` aliased `ReportHttpService` to avoid colliding with the pre-existing `@core/services` reportView-driver `reportService` field, opens the dashboard-report download dialog → `downloadDashboardReport`) | Task 25c: on-demand dashboard report download entry point; re-add the toolbar button + handler after any upstream rewrite of the dashboard toolbar |
| R37 | `ui-ngx/src/app/modules/home/components/home-components.module.ts` | add `DashboardReportDownloadDialogComponent` (Inferrix dialog co-located under `dashboard-page/report/`) to `declarations` + `exports` | Task 25c: registers the on-demand dashboard-report download dialog so `MatDialog.open(...)` resolves it (declared here rather than `ReportModule` because `ReportModule` imports `HomeComponentsModule`). MERGE NOTE: TypeScript compiles fine without this registration — a lost declaration surfaces only at runtime as `MatDialog.open` failing; re-add both array entries after any upstream rewrite of this module's `declarations`/`exports` |
| R38 | `application/src/main/resources/thingsboard.yml` | add `reports:` block (renderer.enabled default false, max_concurrent, browser_path, base_url, generation_timeout_ms, test_report_pool_size) after the `scheduler:` block + `report`/`report_template` entries in `audit-log.logging-level.mask` | Task 26: reporting feature config + audit masks; renderer is opt-in via `REPORTS_RENDERER_ENABLED=true` (default false so browserless contexts/tests boot); re-add both blocks after any upstream rewrite of `thingsboard.yml` |
| R39 | `application/src/test/java/org/thingsboard/server/service/job/JobManagerTest.java` | add `testSubmitJob_reportType_reportTemplateEntityIdAccepted` — a DAO-backed regression proving a `JobType.REPORT` job with a `REPORT_TEMPLATE` entity id passes `DefaultJobService.saveJob` | Task 20 fix-wave: the ONLY test exercising `JobType.REPORT` through the real save path (this class otherwise uses only `JobType.DUMMY`), so it is the regression guard for the R29 entity-type gap; re-add the test method after any upstream rewrite of this pre-existing TB-core test class |
| R40 (R2a) | all module `pom.xml` (60 files) — project `<version>` 4.3.1.3 → **4.3.1.4** (`thingsboard.client.version` property left at 4.3.1.3 — external `thingsboard-ce-client` artifact, not the project version) | R2a Task B0: advances `getPackageSchemaVersion()` so the R2 config-shape data migration (`V4_3_1_4Migration`, R2a spec §3) runs on existing 4.3.1.3 DBs. Boot jar becomes `thingsboard-4.3.1.4-boot.jar`. On upstream merge, re-apply the bump on top of upstream's version (do NOT revert to upstream's) — Inferrix carries a +1 patch version for its migration |
| R41 (R2a) | **NEW FILES** (additive, no TB-core edit): `application/src/main/java/org/thingsboard/server/service/install/lts/V4_3_1_4Migration.java` + `application/src/main/data/upgrade/lts/4.3.1.4/schema_update.sql` (header-only no-op) | R2a Task B1: `LtsMigration` bean (`@Component @TbCoreComponent`, `getVersion()=="4.3.1.4"`) auto-discovered by `LtsMigrationService` via `List<LtsMigration>` injection — **no edit to `LtsMigrationService`**. Its `apply()` rewrites pre-existing R1-flat `report_template.configuration` rows to PE-shape in pure Java/Jackson (root `type:"PDF"`→`format:"PDF"`; flat DASHBOARD `dashboardId`/`state`/`timewindow`→nested `config`, drops `pageWidth`), idempotent (skips rows already having root `format` + every DASHBOARD `config`). The `4.3.1.4/` dir + no-op SQL exist ONLY to satisfy the dir↔bean consistency guard in `LtsMigrationIntegrationTest` (its `SQL_LESS_ALLOWED` set is empty — a bean with no matching dir fails that test). Test: `ReportTemplateConfigMigrationTest`. **Merge recovery:** if upstream ever ships its own `4.3.1.4`/`V4_3_1_4Migration`, rename ours (e.g. `V4_3_1_5`) + bump the project version to match — the version string must stay unique across beans (`LtsMigrationService.validateAndSort` throws on duplicates) |

### Merge-recovery procedure

- **`EntityType.java`** (R1) — verify `REPORT_TEMPLATE(108)` and `REPORT(109)` survive appended after `SCHEDULER_EVENT(103)` (PE-exact proto numbers). Upstream regularly appends entity types here; re-apply after any merge that drops them. Not exhaustively switched over in a compiler-enforced way (report `checkEntityId` arms carry a `default`) — check by hand.
- **`EntityIdFactory.java`** (R2) — verify the two `case REPORT_TEMPLATE`/`case REPORT` arms survive in `getByTypeAndUuid`. This is an exhaustive `switch` expression — a dropped arm fails the `common/data` build loudly, so breakage here is unmissable.
- **`EntityId.java`** (R3) — verify the two `@DiscriminatorMapping` entries (REPORT_TEMPLATE / REPORT) survive on the `@Schema` annotation. **Not compiler-enforced** — Swagger/OpenAPI metadata only; a silent drop compiles cleanly and only degrades the generated API schema for polymorphic report entity ids. Check by hand.
- **`JobType.java`** — verify `REPORT("Report generation")` survives as the last enum constant (comma-swapped with `DUMMY`). Not consumed by name in an exhaustive switch anywhere except `Job.java` (see below); a drop there removes the type outright.
- **`JobConfiguration.java` / `JobResult.java` / `Task.java` / `TaskResult.java`** — verify the `@Type(name = "REPORT", value = Report*.class)` entry survives in each of the four `@JsonSubTypes` lists. **None of these four are compiler-enforced** — `@JsonSubTypes` is annotation metadata Jackson reads reflectively; a merge that silently drops one still compiles cleanly and only fails at runtime, the next time a REPORT job/task payload round-trips through Kafka or the REST API (mis-resolves to the wrong subtype, or Jackson throws on an unrecognized discriminator value). Check all four by hand after every merge touching `common/data/.../job/`.
- **`Job.java`** — verify `case REPORT -> new ReportJobResult();` survives in `presetResult()`'s switch expression. **This one IS compiler-enforced** — an exhaustive switch expression over `JobType` with no `default`; a merge that adds a new `JobType` constant (or drops this arm) fails the `common/data` build outright, so breakage here is unmissable, unlike the four `@JsonSubTypes` rows above.
- **`ModelConstants.java`** — verify the `Report template constants.` block (R10) AND the `Report constants.` block (R11) both survive, back-to-back, directly after the `Scheduler event constants.` block. Not compiler-enforced (plain `String` constants referenced from `@Column(name = ...)` annotations) — a merge that drops either block fails `dao` compilation loudly wherever `AbstractReportTemplateEntity`/`AbstractReportEntity` reference it (unmissable), but a merge that instead reorders/duplicates it fails silently — check by hand.
- **`Resource.java`** (R12) — verify `REPORT_TEMPLATE(EntityType.REPORT_TEMPLATE),` and `REPORT(EntityType.REPORT);` survive as the last two enum constants. **Not compiler-enforced** — `Resource` is not switched over exhaustively anywhere; a merge that silently drops either constant still compiles cleanly and only fails at runtime (a 403, or an `IllegalArgumentException` thrown from `Resource.of()` the next time a report/template entity id is resolved) — check by hand.
- **`TenantAdminPermissions.java`** (R13) / **`CustomerUserPermissions.java`** (R14) — verify the `Resource.REPORT_TEMPLATE`/`Resource.REPORT` `put(...)` lines survive in both files, and that `CustomerUserPermissions` still has its own dedicated `reportPermissionChecker` (with `Operation.CREATE`) and `reportTemplatePermissionChecker` (READ-only) — not silently merged back onto the shared `customerEntityPermissionChecker`, which lacks `Operation.CREATE` (same landmine as Scheduler S14) and would over-grant `WRITE` on templates. Pure map registration; no compile-time enforcement — check by hand.
- **`SystemSecurityService.java` / `DefaultSystemSecurityService.java`** (R15) — verify `createUserAccessToken`/`createUserAccessTokenFromPublicId` survive on both the interface and the impl, and that the impl constructor still carries the `customerService`/`tokenFactory` fields. Partially compiler-enforced: a dropped interface method or constructor field breaks `DefaultSystemSecurityServiceTest`/`ReportAccessTokenTest` loudly. **Not** compiler-enforced: a merge that reverts the method *bodies* to something else (e.g. re-adds PE's `MergedUserPermissions`/`UserPermissionsService`, or swaps `userService.findUserAuthDetailsByUserId` for the `UserAuthDetailsCache`) still compiles — re-verify by hand that both bodies still build `SecurityUser` via the 3-arg `(User, boolean, UserPrincipal)` constructor and that `createUserAccessTokenFromPublicId` still passes the real `tenantId` (not `SYS_TENANT_ID`) into `customerService.findCustomerById`.
- **`application/pom.xml`** (R16) — verify the `com.microsoft.playwright:playwright` dependency survives in `<dependencies>`. Not order-dependent (no compiler enforcement either — a dropped dependency only surfaces as a compile failure in the renderer package) — re-append anywhere in the block if missing.
- **`app.component.ts`** (R17) — verify `this.reportService.loadReportParams();` survives in `setupAuth()`, ahead of `this.authService.reloadUser();`, and that the constructor still injects `ReportService`. **Not compiler-enforced** (a dropped call still compiles) and **fails silently at runtime**: a report-mode page would never install the `message` listener or signal `postWebReportResult`, so every headless render hangs until the *renderer's own* (Task 13) external timeout fires — nothing in CE logs the gap. Check by hand after any merge touching `app.component.ts`.
- **`dashboard-page.component.ts`** (R18) — verify all four pieces survive: the `ReportService` constructor injection, the `reportTimewindow`-preferring assignment in `init()`, **both** `reportService.onDashboardLoaded(...)` calls (one per `mobileService.onDashboardLoaded` call site), and the `reportWidgetsCount()` helper. **Not compiler-enforced.** Two distinct silent-failure modes: dropping an `onDashboardLoaded` call leaves `ReportService.lastWaitWidgetsTimeMs` at `0` forever, so `waitForReportPage` times out ("Wait for report page timed out!") on **every** report render; dropping the `reportTimewindow` override is worse because it does **not** time out — the report silently renders the dashboard's own saved timewindow instead of the requested one (wrong data, not a failure). Check by hand.
- **`telemetry-websocket.service.ts`** (R19) — verify the `ReportService` constructor param, both `trackReportWsCommand(...)` call sites (`subscribe()` and `update()`), and the `onWsCmdUpdateMessage(...)` call at the top of `processOnMessage()` all survive. **Not compiler-enforced.** Two asymmetric silent-failure modes: dropping the *send*-side hook (`trackReportWsCommand`) means `receiveWsData` never gets populated, so `waitForWebsocketData`'s `!receiveWsData.size` check is vacuously true and the predicate resolves **immediately** — a false-positive "ready" that can capture a report before real telemetry has streamed in; dropping the *receive*-side hook (`onWsCmdUpdateMessage`) means registered cmdIds never flip to `true`, so the predicate instead **hangs to timeout** ("Wait for websocket data timed out!"). Check by hand — this file has no test coverage to catch either regression.
- **`map.ts` / `geo-map.ts` / `image-map.ts`** (R20/R21/R22) — verify `TbMap`'s `reportService`/`mapUuid` fields and the constructor's `onWaitForMap()` + `tb-web-report` class registration (R20) survive, and that both per-provider `.once('load', ...)` → `onMapLoaded(this.mapUuid)` hooks (R21 tile layer, R22 image overlay) survive. **Not compiler-enforced**, and — like R19 — the two halves fail in **opposite** directions if dropped independently: losing R20 alone (register) means the map is never added to the wait-for-map set, so `waitReportWidgets` returns success without ever having waited for tiles (false positive — blank/gray map tiles in the capture); losing R21/R22 alone (complete) means the map's `mapUuid` is registered but never removed, so `waitForWidgetsLoaded` always hangs to timeout ("Wait for map tiles timed out!") on any dashboard containing a map widget. Check by hand; no test coverage here either (would need a real Leaflet map + tile server). **Known R1 limitation:** only the current `lib/maps/*` (`TbMap`) hierarchy is hooked; the older `lib/maps-legacy/*` widgets (still reachable via `widget-component.service.ts`/`trip-animation.component.ts`) are NOT registered into the wait-for-map set, so a report on a dashboard using a legacy-map widget may capture before its tiles finish loading (blank/partial tiles). Hooking `lib/maps-legacy/*` is deferred — revisit if a legacy-map dashboard must render faithfully.
- **`angular.json` / `package.json` / `yarn.lock`** (R23) — verify the `"test"` architect target and the 7 karma/jasmine `devDependencies` survive. Unlike R17–R22, this is **inert dev tooling**, not a production code path — a silent drop just means `yarn test` fails loudly again ("Cannot find 'test' target") the next time anyone runs it, with no runtime/production impact. Re-run `yarn install` to regenerate `yarn.lock` if it's dropped or garbled (same recipe as Scheduler ledger row S23).
- **`NotificationRequestConfig.java`** (R24) — verify `private List<ReportId> reports;` survives directly after `sendingDelayInSec`. **Not compiler-enforced** — plain `@Data` field, no `@JsonSubTypes`/exhaustive-switch dependency anywhere; a merge that silently drops it still compiles `common/data` and `application` cleanly. Silent-drop symptom: `ReportJobProcessor.onJobFinished` still dispatches the "report generated" notification successfully (`NotificationRequestConfig.setReports` would just no-op / fail to compile loudly only if the setter itself vanished), but the finished report is never attached/linkable from the notification — check by hand after any merge touching `common/data/.../notification/`.
- **`TbEmail.java`** (R25) — verify `private final List<ReportId> reports;` survives as the last field. **Not compiler-enforced** (plain `@Data`/`@Builder` field) — a merge that silently drops it fails `EmailNotificationChannel` (R27) to compile loudly (its `.reports(...)` builder call vanishes a method), so this specific drop is actually self-catching in practice as long as R27 survives; the risk is only if R25 AND R27 are both reverted together, in which case both compile cleanly and email attachments just silently stop.
- **`DefaultMailService.java`** (R26) — verify all four pieces survive: the `reportService` field (constructor param — Spring wiring fails loudly at context-startup if the `ReportService` bean type disappears from the classpath, since `@RequiredArgsConstructor` regenerates the constructor from whatever fields remain), the extended `multipart` condition, the `tbEmail.getImages() != null` guard around the pre-existing images loop, and the reports attachment loop. **Not compiler-enforced** beyond the field itself — two distinct silent-failure modes if a merge reverts pieces independently: dropping the `multipart` extension (b) while keeping the reports loop (d) means a reports-only email is built non-multipart and `helper.addAttachment(...)` throws `MessagingException` at send time (loud, but only at runtime); dropping the images-loop null-guard (c) while keeping (b) reintroduces the NPE described in R26's Why column the first time a reports-only (no-images) email is sent — this is the most dangerous regression to watch for, since a merge that "keeps our reports loop" but silently drops just the one-line null-guard looks fine in a diff review and breaks on exactly the input this feature exists for. Check by hand after any merge touching this file.
- **`EmailNotificationChannel.java`** (R27) — verify `ctx.getRequest().getAdditionalConfig()` and the null-guarded `.reports(...)` builder call survive. **Partially compiler-enforced**: dropping R25 (`TbEmail.reports`) fails this file to compile. **Not enforced**: a merge that "simplifies" the null-guarded ternary to `.reports(ctx.getRequest().getAdditionalConfig().getReports())` compiles fine and only NPEs at runtime, the first time a notification with no report attachment (i.e. almost all of them) is emailed. Check by hand.
- **`application/src/main/java/org/thingsboard/server/controller/BaseController.java`** (R28) — Two `@Autowired` report service fields (`reportService`, `reportTemplateService`); `checkReportId`/`checkReportTemplateId` helpers; `case REPORT`/`case REPORT_TEMPLATE` arms in `checkEntityId`. Task 18 — mirrors Scheduler S15 wiring for the report REST controllers (Tasks 19–21). MERGE NOTE: the `checkEntityId` switch has a `default` arm, so a lost REPORT/REPORT_TEMPLATE arm will NOT break the build — it silently routes to `entitiesService.findEntityByTenantIdAndId`; re-add both arms by inspection after any upstream merge that rewrites this switch.
- **`Job.java`** (R29) — verify `EntityType.REPORT_TEMPLATE` survives in `SUPPORTED_ENTITY_TYPES` (alongside `DEVICE`/`ASSET`/`DEVICE_PROFILE`/`ASSET_PROFILE`). **Not compiler-enforced** — a plain `Set.of(...)` referenced only from `DefaultJobService.saveJob`'s runtime validation; a merge that silently drops it still compiles cleanly and only fails at runtime, the first time any `JobType.REPORT` job (scheduled via `InferrixSchedulerReportExecutor` or on-demand via `ReportController#requestReport`) is submitted — `IllegalArgumentException: Unsupported entity type ...` from `saveJob`. Distinct from R9 (the `presetResult()` switch arm) — both are required, independently droppable, and fail differently (R9 loss is a compile error; R29 loss is a runtime submit-time failure). Check by hand after any merge touching this file.
- **`entity-type.models.ts`** (R30) — verify all three insertion points survive: the `REPORT`/`REPORT_TEMPLATE` `EntityType` enum members, the `entityTypeTranslations` map entries (both keyed after `SCHEDULER_EVENT`), and the `entityTypeResources` map entries (`helpLinkId: 'reports'` / `'reportTemplates'`, also after `SCHEDULER_EVENT`). None of these are compiler-enforced against `EntityType`'s members (both maps are populated ad hoc, not exhaustively) — a merge that reorders or drops an entry fails silently (blank/untranslated label or missing help link in the report UI) — check by hand. Same landmine class as Scheduler's own S17 row above.
- **`public-api.ts`** (R31) — verify `export * from './report.service'` survives (placed between `queue.service` and `resource.service`, alphabetical). Upstream doesn't touch this file's content (Inferrix-only exports), but a merge could still drop the line if conflict resolution goes wrong — check by hand.

---

## Inferrix schema overlay (DB migration mechanism)

**The single, Inferrix-owned channel for all custom DDL.** One file holds every Inferrix table / column / index; four one-line seams feed it into all three schema consumers (fresh install, upgrade, DAO tests). Adding a future DB change = append idempotent DDL to that one file — no upstream ThingsBoard schema file changes again, and no new ledger row. (Chosen 2026-07-15 over Liquibase and over per-table appends to upstream `schema-entities.sql`. Upstream Liquibase is **not** used on this fork; this overlay + the TB-native install/upgrade app is the only DB-migration path.)

- **Overlay file (additive, Inferrix-owned):** `dao/src/main/resources/sql/schema-inferrix.sql`
  - **Idempotent only** (`CREATE ... IF NOT EXISTS`, guarded `ALTER`/`DO` blocks) — it is re-applied on **every** fresh install AND **every** upgrade, so non-idempotent statements would fail the second run.
  - Placed in `dao/src/main/resources/sql/` so one physical file reaches all three consumers: it is (a) on the DAO test classpath and (b) wildcard-copied (`<include>**/*.sql</include>` in the root `pom.xml` `copy-data` execution) into the install data dir at `<dataDir>/sql/`.
  - Current content: `scheduler_event` table + `idx_scheduler_event_originator_id` (relocated here from the retired S4/S5/S6).

### TB-core seams (feed the overlay to each consumer)

| # | File | Change | Why |
|---|------|--------|-----|
| O1 | `application/.../service/install/SqlEntityDatabaseSchemaService.java` | Add `SCHEMA_INFERRIX_SQL` constant; override `createDatabaseSchema(boolean)` to call `super.createDatabaseSchema(...)` then `executeQueryFromFile(SCHEMA_INFERRIX_SQL)` | Fresh-install create path applies the overlay after the base entity schema (reads `<dataDir>/sql/schema-inferrix.sql`) |
| O2 | `application/.../service/install/SqlDatabaseUpgradeService.java` | Add `SQL_DIR` + `SCHEMA_INFERRIX_SQL` constants; in `upgradeDatabase()`, after `loadSql(getSchemaUpdateFile("basic"))`, add `loadSql(Paths.get(installScripts.getDataDir(), SQL_DIR, SCHEMA_INFERRIX_SQL))` | Upgrade path re-applies the overlay on every upgrade — our own upgrade channel, decoupled from upstream's churny `basic/schema_update.sql` |
| O3 | `dao/src/test/java/.../PostgreSqlInitializer.java` | Add `"sql/schema-inferrix.sql"` to `sqlFiles` (after `schema-entities-idx-psql-addon.sql`) | `@DaoSqlTest` bootstrap creates the overlay tables (classpath load) so DAO tests see them |
| O4 | `dao/src/test/java/.../TimescaleSqlInitializer.java` | Same insertion as O3 | Timescale test-profile parity |

### Merge-recovery procedure

- **Overlay file** — Inferrix-only additive file; upstream never touches it. Verify it still exists and its DDL is intact.
- **O1–O4 seams** — all four must survive. O1's override signature is compiler-checked against `DatabaseSchemaService` (a drop is a build error). O2/O3/O4 are **not** compiler-enforced: O2 dropped → upgrades silently skip the overlay; O3/O4 dropped → DAO tests fail loudly with `relation "scheduler_event" does not exist` (self-catching). After any merge touching these files, grep each for `schema-inferrix` / `SCHEMA_INFERRIX_SQL`.
- **`copy-data` (root `pom.xml`)** — verify the `../dao/src/main/resources` resource with `<include>**/*.sql</include>` survives, so the overlay keeps flowing into `<dataDir>/sql/`. Upstream owns this execution; a narrowing of the include pattern would strand the overlay at install/upgrade time (tests, which read the classpath, would still pass and mask it) — check by hand.
- **Adding future Inferrix DDL:** append idempotent statements to `schema-inferrix.sql`. Nothing else changes — no new seam, no upstream-file touch, no new ledger row (optionally extend the overlay file's "Current content" note above).

---

## How to extend this ledger

When you modify a TB-core file for a new feature:

1. Add a `## Feature: <name>` section with the same structure (module path, status, files table).
2. List each modified file with a precise change anchor (line region, block name, or insertion point). Avoid copy-pasting whole diffs — they rot fast against upstream churn. Describe the *intent* of the patch so a future reader can reapply by hand.
3. Add the file to the merge-recovery procedure at the bottom of that section.

Keep this file in sync with `master` and `inferrix-release-4.3` (cherry-pick across branches as needed).
